# Fleet configuration for cert-manager on rancher-manager cluster
# takeOwnership: adopt pre-existing cluster resources (e.g. manually applied HelmChartConfig)
helm:
  takeOwnership: true

targetCustomizations:
  - name: local
    clusterSelector:
      matchLabels:
        management.cattle.io/cluster-display-name: local
